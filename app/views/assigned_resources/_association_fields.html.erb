<% [:daily_pass, :monthly_pass, :voice_mail, :clothing, :hygiene].each do |association| %>

	<% fields_for_association(@assigned_resource, association, @assigned_resource.resource_type)  do |subform| %>
	
		<%= render :partial=>"assigned_resources/associations/#{association.to_s}_form", :locals=>{:subform=>subform} %>
	
	<% end %>

<% end %>
