
<% form_for([@client,@phone]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial=>'phones/form', :locals=>{ :f => f } %>
  <p>
    <%= f.submit 'Create' unless request.xhr? %>
	<%= submit_to_remote 'update_ajax_btn', 'Create', :url=>{:action=>'create'} if request.xhr?%>
  </p>
<% end %>

<br/>