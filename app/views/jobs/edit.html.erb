
<% form_for([@client, @job]) do |f| %>
  <%= f.error_messages %>
  <%= render :partial=>'jobs/form', :locals=>{ :f => f }%>
  <table>
    <tr>
	  <td>
	    <%= f.submit 'Update' unless request.xhr? %>
		<%= submit_to_remote 'update_ajax_btn', 'Update', :url=>{:action=>'update'} if request.xhr? %>
  	  </td>
  	</tr>
  </table>
<% end %>

<br/>